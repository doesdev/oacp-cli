{
  "name": "{{name}}",
  "oacp": {
    "env": {},
    "namespace": "{{namespace}}",
    "access": "lib/access.js",
    "jwt": {
      "privKeyPath": "config/{{namespace}}.priv",
      "pubKeyPath": "config/{{namespace}}.pub",
      "issuer": "{{name}}"
    }
  },
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "test/index.js"
  },
  "dependencies": {
    "oacp": "musocrat/oacp"
  }
}
